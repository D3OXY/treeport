# Changesets

Add a changeset for every user-facing change:

```bash
pnpm changeset
```

Merging to `main` opens or updates a version PR. Merging that version PR publishes to npm through GitHub Actions.
